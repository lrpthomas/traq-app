/**
 * useSync Hook
 * =============================================================================
 * Re-export from SyncContext for convenience
 *
 * @example
 * const { status, pendingCount, triggerSync, queueAssessment } = useSync()
 */

export { useSync } from '@/contexts/SyncContext'
