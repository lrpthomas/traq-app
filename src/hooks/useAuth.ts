/**
 * useAuth Hook
 * =============================================================================
 * Re-export from AuthContext for convenience
 * Use this hook in components to access auth state and methods
 *
 * @example
 * const { user, isAuthenticated, signIn, signOut } = useAuth()
 */

export { useAuth } from '@/contexts/AuthContext'
