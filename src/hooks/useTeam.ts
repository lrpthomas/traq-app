/**
 * useTeam Hook
 * =============================================================================
 * Re-export from TeamContext for convenience
 * Use this hook in components to access team state and methods
 *
 * @example
 * const { currentTeam, teams, selectTeam, canEdit } = useTeam()
 */

export { useTeam } from '@/contexts/TeamContext'
